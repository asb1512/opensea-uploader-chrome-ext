// locates extension's start button
let startBtn = document.getElementById('start-btn');

// adds event listener to start button
startBtn.addEventListener('click', function() {
  startBtn.style.backgroundColor = 'green';
});
